import React from "react";

const CheckoutSectionTitle = ({ title }) => {
    
    return (
        <div className="ship-info--title">
            <h2 className="checkout-title checkout-section-title">{title}</h2>
        </div>
    );
};

export default CheckoutSectionTitle;
